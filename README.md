# Inicialização

Para inicializar o projeto, segue assim:

```bash
git clone <URL_DO_REPOSITORIO>
cd <NOME_DO_PROJETO>
npm install
```

Depois de instalar as dependências, você pode iniciar o servidor de desenvolvimento com:

```bash
npm run dev
```